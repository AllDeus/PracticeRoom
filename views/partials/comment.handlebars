<div class="comments">
    {{!-- {{#each this}} --}}
    {{#each comments as |comment| }}
    <section class="comment">
        <div class="meta">
            {{user.displayName}} on {{format_date date_created}}
        </div>

        <div class="text">
            {{content}}
        </div>
    </section>
    {{/each}}
</div>